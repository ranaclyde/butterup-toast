---
import { Code } from 'astro/components'

const code = `
  import { butterup } from 'butterup-toast'

  // Max number of toasts that can be on the screen at once
  butterup.options.maxToasts = 3

  // How long a toast will stay on the screen before fading away
  butterup.options.toastLife = 3000

`
---

<section class="max-w-3xl mx-auto my-10">
  <h3 class="text-2xl font-semibold">Other configuration</h3>
  <p class="mt-2">
    ButterupToast provides some extra options that can be configured globally in
    your application.
  </p>
  <div class="flex flex-col items-center">
    <Code
      code={code}
      lang="ts"
      theme="dracula"
      class="my-5 rounded-xl w-full text-sm lg:text-base"
    />
  </div>
</section>
